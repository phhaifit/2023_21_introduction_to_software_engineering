# OpenClaw Provisioning Jobs

Use these jobs for workspace runtime provisioning, resize, shutdown, and deletion.

The worker must call the backend OpenClaw runtime adapter boundary rather than duplicating container logic in feature modules.
