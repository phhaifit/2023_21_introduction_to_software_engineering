# End-to-End Tests

E2E tests will be added after feature modules have runnable UI/API behavior.

Planned flows:

- Register, subscribe, create workspace, invite member, create agent, run task.
- Upload document, index it, permit an agent, retrieve knowledge during task execution.
- Create workflow, trigger it, aggregate final result.
