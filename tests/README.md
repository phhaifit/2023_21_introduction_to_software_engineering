# Tests

Test layers:

- `contract`: fast checks for shared contracts, module boundaries, API/event schemas, and required scaffolding.
- `e2e`: browser or API flows that validate integrated product behavior after feature modules exist.

Run current foundation checks:

```bash
npm run test:contracts
```
